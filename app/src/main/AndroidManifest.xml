<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.canonicalexamples.leagueApp">

    <application
        android:name=".app.LeagueAppApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LeagueApp">
        <activity
            android:name="com.canonicalexamples.leagueApp.view.LeagueActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.LeagueApp.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".view.ServerPanelActivity"
            android:theme="@style/Theme.LeagueApp.NoActionBar">
        </activity>
    </application>

</manifest>
